import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.binhdq',
  appName: 'mythirdapp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
